import { AbstractControl } from "@angular/forms";

export function Validators<%= h.changeCase.paramCase(name) %>(
    control: AbstractControl
): { [key: string]: any } | null {

    // validator

    // return result;
}